# Independent review of candidate d348e84

Reviewed 2026-09-24 UTC. Decision: **revise**. The mathematical reduction is sound on the stated positive-integer domain, but the executable forward map fails on legal inputs with sufficiently large binary-encoded integers. Repair that domain failure and rerun the targeted check before advancing.

## Correctness

The current proof in `work/proof.md:20-26` covers the crucial quantifiers. Every feasible schedule starts with `M`, and all `E_i` occur before `H`. If `P` is the set of late tasks before `H`, the separator deadline gives `w(P) <= floor(A/2)`. The total-work and forced reentry-setup bound gives `A-w(P) <= floor(A/2)`. These force even `A` and `w(P)=A/2`, regardless of batching order or extra setup charges. Conversely, the displayed batched schedule meets each deadline for any partition witness. These arguments show target feasibility iff source feasibility, so both feasible schedules and target `NO-SOLUTION` recover valid source outputs. I found no missing premise in these schedule inequalities. The first task's zero setup and subsequent destination-compiler charges agree with `work/contract.md:5` and `work/algorithm.py:16-23,27-32`.

The construction has `2n+2` tasks, `n+1` compilers, and four deadline values (`1,T,2T,D`). Numerical values have `O(log n + log A)` bits. The extraction scans an encoded permutation and returns precisely the late-task indices before `H`; both maps are deterministic and require polynomial bit time in the mathematical integer model. The `work/proof.md:30` claim that serialization is polynomial on every admitted input, however, needs the executable implementation to handle every such integer.

**Executable-domain defect.** `work/contract.md:3` admits positive integers of arbitrary finite bit length in decimal JSON. `work/algorithm.py:37-39` uses Python's default `json.load`/`json.dump` integer conversion, which is capped at 4300 decimal digits in the locked Python 3.12 runtime. The independent check `reviews/initial/check_large_integer.py` passes a legal two-item source with equal 4301-digit weights (a YES instance). Command: `uv run python campaigns/partition-deadlines-setup-times/reviews/initial/check_large_integer.py`. Observed `exit=1`, with `Exceeds the limit (4300 digits) for integer string conversion: value has 4301 digits`. Thus F does not produce a legal target for this x; no recovery call is possible. This is a runtime representation limit, not an exponential-size input (the two weights use about 28.6k bits together). Repair the CLI's JSON integer input and output handling for arbitrary finite integers, including `--extract` parsing, while retaining the same problem domain. Then rerun this check and a representative positive and negative injected-instance loop.

The recorded full loops in `work/verification.md` cover 100 prepared inputs/128 outputs and a separate 84-input/106-output brute-force family, with positive, negative, and alternate target schedules. I inspected `work/check.py` and `work/verify.py`: both independently validate recovered source witnesses against subset sums and solve the injected target instances without invoking the candidate as an oracle. Their finite domains do not encounter the digit limit and do not replace the general proof. I did not rerun either whole suite.

## Novelty

The hardness direction is **known**, and the fixed question explicitly permits reconstructing a published construction. [Garey and Johnson, *Computers and Intractability*, Appendix A5.1, SS6, p. 238](https://perso.limos.fr/~palafour/PAPERS/PDF/Garey-Johnson79.pdf) gives the same scheduling definition and labels the result a transformation from PARTITION. [Bruno and Downey, *SIAM Journal on Computing* 7(4), pp. 393–404 (1978)](https://epubs.siam.org/doi/10.1137/0207031) is the cited primary paper; its accessible abstract states polynomial reductions and the NP-completeness boundary, but the full proof was behind access control. The current separator gadget was not compared line by line with that original proof, so novelty of the particular gadget remains unresolved. The current `work/proof.md:30` appropriately does not claim it. Literature search date: 2026-09-24 UTC. This coverage supports a rule-completion contribution, not a new hardness classification or a claim of an original gadget.

## Significance

The fixed acceptance criterion asks for a complete reproducible F/G rule, proof, and executable checks, rather than an open complexity result. Once the CLI domain defect is fixed, this candidate supplies a concrete witness decoder and a robust argument against alternative batching orders, which the [upstream issue's proposed construction](https://github.com/CodingThrust/problem-reductions/issues/474) leaves unspecified and illustrates with a non-witness. The contribution is the explicit reproducible reconstruction. Overhead is linear in item count for tasks and compilers and polynomial in encoded bit length; this establishes theoretical suitability. The exact target solver is exponential in the number of tasks in the worst case, and `work/verification.md` reports no scaling measurements beyond eight source items. There is no supported practical-speed claim and no question-specific resource ceiling that would block advance after correctness repair.

## Isolation and model route

The reviewer was started with the registered `research-reviewer` role in a fresh context. In this session, the effective write boundary and no-spawn rule were instructions only: tools were not denied, no enforced depth cap was visible, and filesystem sandbox mode was `danger-full-access` with approval policy `never`. I wrote only `reviews/initial/review.md` and `reviews/initial/check_large_integer.py`; I did not edit the candidate, its tests, or prior evidence. The model route inherited the parent GPT-6 route with no override; the exact model identifier was not exposed in this reviewer context. These isolation facts do not support the correctness judgment by themselves.
